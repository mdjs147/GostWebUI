# 安全政策

## 报告安全问题

如果仓库已启用 GitHub Private Vulnerability Reporting，请优先通过仓库的 **Security → Advisories → Report a vulnerability** 私下报告。

如果该入口不可用，可以创建不含利用代码、真实地址、凭据或用户数据的公开 Issue，只说明需要私下联系维护者处理安全问题。维护者建立私密沟通渠道前，请不要公开完整细节。

报告中建议包含：

- 受影响版本或提交
- 影响范围与攻击前提
- 最小复现步骤
- 建议修复方向（如有）
- 已确认泄露的资料类型，但不要附带真实秘密

本项目目前不承诺固定响应时限；维护者确认问题后会根据影响范围协调披露与修复。

## 支持范围

安全修复以当前默认分支和最新公开版本为主。已停止维护的历史版本不保证获得补丁。

## 重要安全边界

- Web 管理服务设计为仅监听回环地址，不应直接暴露到局域网或公网。
- `config.json`、`rules.json` 和日志可能包含目标地址、代理位置及其他环境信息，不应提交或随公开发行包分发。
- `gost.exe` 是用户自行获取的外部程序。应从 [GOST 官方 Releases](https://github.com/go-gost/gost/releases) 下载，并在接受新的 SHA-256 指纹前核实来源。
- 指纹锁定用于发现文件变化，不是代码签名，也不能抵御已能修改程序目录和配置文件的同账户攻击者。
- MySQL 模式只透传 TLS，不验证远端业务身份，也不替代客户端证书校验、凭据保护或数据库授权。

第三方组件及责任边界见 [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md)。
